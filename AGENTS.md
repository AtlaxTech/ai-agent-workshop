# AI Agent Workshop 协作规则

本仓库用于一周 Applied AI Agent Engineering 基础强化。所有 Agent 必须遵守本文件；若任务与规则冲突，先停止并向用户说明冲突，不得自行扩展权限。

## 1. 启动读取顺序

每次进入仓库只按顺序读取：

1. `README.md`
2. `00-路线与状态/当前状态.md`
3. `00-路线与状态/学习系统.md`
4. `00-路线与状态/路线索引.md`
5. `00-路线与状态/一周学习路线图.md`
6. `00-路线与状态/学习进度.md`
7. 用户点名的当天笔记、问题或实验

不要默认全仓扫描。只有当前任务确实需要时，才读取相关输入、练习、项目或复习文件。

## 2. 核心权限边界

- 所有操作由用户明确指令触发；不得自动安排、总结、出题、整理、调整计划、提交或推送。
- 除非用户明确创建定时任务，否则时间经过、日期变化或发现新文件都不是行动授权。
- 一条指令只授权对应动作。例如“整理 Day 1”不授权自动出题、调整 Day 2、commit 或 push。
- `执行今日学习收尾 Day N` 是用户已定义的复合指令，只授权本文件中列出的收尾流水线；仍不授权实际复习、调整下一天、commit 或 push。
- 每次只处理用户点名的 Day、主题或文件；完成后停止，等待下一条指令。
- 不推进 AtlasOps、简历、LinkedIn、求职投递或其他仓库工作。
- 不读取或记录 `.env`、Token、API Key、密码、私钥及其他敏感信息。

## 3. 事实与进度规则

- 路线是计划，不是完成记录。
- 只有用户提供真实学习时间、代码运行结果、答案、复述或实验输出后，才能更新完成度。
- Agent 生成或协助生成的代码属于用户主导的学习与项目产物；“掌握程度”另外依据用户能否解释、复现、修改和判断结果记录。
- 不因看完视频自动判定掌握，不因一次答错否定整个能力域。
- 不虚构学习时长、正确率、测试结果、性能数据或项目状态。

## 4. 目录职责

- `01-输入区/`：用户原始输入。整理时不得覆盖、改写或删除原文。
- `02-每日笔记/`：当天工作台，记录计划、过程、问题和结论。
- `03-整理区/`：经核对后可复用的稳定知识；必须能追溯到输入或实验。
- `04-练习区/`：题目、用户答案、标准答案、解析和错题。
- `05-项目区/`：可运行代码与实验；每个项目必须有 README、运行方式和成功判断。
- `06-复习区/`：复习卡片、错题回访、口述提纲和未掌握清单。
- `07-总结与评估/`：每日总结、阶段复盘、能力缺口和后续建议。
- `00-路线与状态/`：权威路线和进度；只根据已确认事实更新。

不要在多个目录复制同一正文。使用相对链接连接原始记录、整理知识、练习和实验。

用户可以在输入、每日笔记、整理、练习、项目和总结区域任意输入。复习索引、今日队列、知识卡片和已掌握目录是状态机管理路径，不作为普通 Ingest 输入。Agent不得因为位置不标准而忽略内容，也不得未经判断机械移动；先通过处理游标识别变化，再按职责保留原文或创建派生页。

## 5. 指令协议

### `安排 Day N`

1. 读取路线、当前状态、进度和上一日遗留项。
2. 读取现有 `02-每日笔记/Day-N/学习记录.md`，在同目录生成 `当天计划.md`。
3. 保持总有效学习时间约 8 小时；上一日补洞最多 45 分钟。
4. 每个任务必须包含目标、材料、动手产出、完成判断和预计时间。
5. 只安排当天，不自动开始教学或生成下一天计划。

### `回答问题：...`

1. 先判断问题属于概念、代码、报错、设计还是结果判定。
2. 使用“直觉解释 → 准确定义 → 最小例子 → 常见误区 → 理解检查”。
3. 不用大量新术语解释旧术语；涉及代码时先给最小可运行示例。
4. 只有用户同时说“记录问题”时，才写入当天笔记或输入区。

### `整理 Day N`

1. 只读取 Day N 的原始输入、每日笔记和实验输出。
2. 保留原始内容，在 `03-整理区/` 写整理版本并建立来源链接。
3. 标记：已确认、待验证、错误理解、仍有疑问。
4. 补充必要注释、最小示例和概念关联，但不得编造用户已完成内容。
5. 在 `07-总结与评估/每日总结/` 写当日总结并更新真实进度。
6. 不自动出题或调整下一天，除非指令明确包含这些动作。

### `整理未归档输入`

1. 运行 `python3 scripts/learning_ops.py scan` 获取相对上次 checkpoint 的变化范围。
2. 逐项判断：原始来源、每日过程、稳定知识、练习、项目证据、复习材料或总结。
3. 原文保留；优先更新已有稳定知识页，补来源链接、概念关联、冲突和待验证标记。
4. 更新相关 README 或索引；不为了形式创建空页。
5. 运行 `lint`，处理完毕后运行 `checkpoint`。
6. 不更新用户未提供的学习时间、正确率或掌握结论。

### `执行今日学习收尾 Day N`

按固定顺序执行，不拆成需要用户重复授权的七个小步骤：

1. `scan` 识别新增、修改与删除内容。
2. 执行 Ingest：保留原始输入，归位或建立派生页，更新稳定知识和链接。
3. 整理 Day N，写入 `07-总结与评估/每日总结/`，更新真实进度。
4. 从当天内容中选择值得长期记忆的原子知识；先运行 `next-id`，按求职/项目关键、当前阶段核心、补充标注 3/2/1 级优先级，再按模板创建卡片并执行 `register`。
5. 运行 `due --date YYYY-MM-DD --write`，按每日预算与优先级生成今日复习区；超预算项必须保持原到期日。
6. 运行 `lint`；无待确认的机械问题后执行 `checkpoint`。
7. 按 `模板/每日收尾报告模板.md` 报告创建、更新、待复习和未执行事项，然后停止。

此指令不自动向用户提问复习题，不自动调整 Day N+1，不执行 Git 操作。

### `出题 Day N` 或 `出题：<主题>`

1. 只根据用户实际学过的内容生成 10–20 道题。
2. 默认组合：概念判断、代码阅读、输出预测、错误定位、场景设计。
3. 题目与答案分区，先题后答案；解析必须说明为什么，以及错误选项/思路错在哪里。
4. 将题目存入 `04-练习区/Day-N/`，用户答案追加到独立区，不覆盖题目。
5. 错题进入 `06-复习区/错题与薄弱项.md`；不自动把正确率写成能力结论。

### `复习 Day N` 或 `复习：<主题>`

1. 读取相关整理笔记、错题和未掌握项。
2. 优先主动回忆、输出预测和故障判断，不只是重复阅读。
3. 生成 30–60 分钟复习任务；结束后等待用户回答再更新结果。

### `生成今日复习任务`

1. 使用用户当地日期运行 `due --date YYYY-MM-DD --write`。
2. 根据配置的 25 分钟默认预算筛选队列，依次优先：3 级求职/项目关键、历史失败更多、逾期更久、阶段更低。
3. 超出预算的卡片列为暂缓，但仍保持到期/逾期，不重新排期、不标记完成。
4. 只生成队列，不读取答案、不标记完成、不改变排期。

### `开始今日复习`

1. 读取当天队列，若不存在则先生成。
2. 一次只读取一张卡片的问题区并向用户提问；用户回答前不得展示参考答案。
3. 记录用户原始回答、是否查看资料、提示程度和信心。
4. 全部完成前不调用 `record`，避免把中断会话误记为已复习。

### `完成今日复习并重新排期`

1. 根据可观察表现为每张卡片选择 `again / hard / good / easy`，依据见 `00-路线与状态/学习系统.md`。
2. 逐项运行 `record`，由脚本计算日期和归档条件；Agent不得手算后直接编辑索引。
3. 在 `06-复习区/复习记录/YYYY-MM-DD.md` 保存回答、评级和理由。
4. `again` 和 `hard` 项加入错题与薄弱项；归档项由脚本移动到 `已掌握/`。
5. 运行 `lint` 并报告新的下次日期。一次复习不得直接手工归档。

### `检查学习系统`

1. 运行 `scan`、`lint` 和到期项查询。
2. 检查无来源整理页、重复卡片、孤立知识、冲突、断链、逾期项和未处理输入。
3. 机械错误可以修复；语义取舍、冲突事实或不确定归类必须询问用户。

### `阶段复盘`

1. 汇总真实投入、完成产出、练习结果、实验和故障记录。
2. 分析已建立能力、证据不足、知识缺口和反复错误模式。
3. 给出按优先级排序的补强方案，并说明依据。
4. 写入 `07-总结与评估/阶段复盘/`，但不自动改变总路线。

### `执行 Day 3 累积口述验收` / `执行 Day 7 累积口述验收`

1. 读取 `00-路线与状态/累积口述验收.md` 和对应 Day 工作台。
2. 只展示题目与时间要求，不先展示示范答案或评分结论。
3. 用户完成口述或提供逐字稿后，按五维量表逐项引用证据评分；不依据表达自信程度猜测技术正确性。
4. 保存原始口述、评分、误区和补强动作到 `07-总结与评估/累积口述/Day-N.md`。
5. 未通过项进入薄弱项和后续复习；不得伪造通过，也不得自动调整下一天计划。

### `调整 Day N`

1. 只能调整尚未完成的目标日。
2. 依据此前真实进度、错题、实验和时间偏差进行调整。
3. 说明保留、删减、新增和延后的内容及原因。
4. 不降低核心验收标准，不把遗漏内容无限堆到下一天。

## 6. 笔记整理标准

整理后的知识页至少包含：

- 一句话结论；
- 直觉解释和准确定义；
- 最小代码或流程示例；
- 与 Agent 工程的关联；
- 常见误区或失败模式；
- 来源与关联实验；
- 复习问题。

对不确定内容明确写“待验证”，必要时查阅官方文档。不得用语言流畅度代替事实核验。

只有适合主动回忆且值得长期调用的知识才创建复习卡片。卡片必须原子化、可判定、可追溯，并尽量包含应用或变体；不要把整段摘要复制成问答。

## 7. 代码与实验标准

- 优先 Python 3.11+、类型提示、清晰命名和最小依赖。
- 实验必须记录安装、运行命令、输入、预期输出、实际输出和成功判断。
- 修改前先阅读当前项目 README；失败时保留关键错误和排查过程。
- 不以“模型返回了一段文字”作为唯一成功标准；还要检查格式、证据、工具结果、状态变化或测试断言。
- 不把演示数据描述为生产指标。

## 8. Git 规则

- 不修改远端历史，不强制推送，不删除分支。
- commit 与 push 分别需要用户明确指令。
- 提交前检查状态和 diff，只暂存当前任务文件；不要使用 `git add .`、`git add -A` 或 `git add --all`。
- 不提交 `.env`、虚拟环境、模型、缓存、数据库、日志和系统文件。
- 默认提交信息格式：`学习(Day N): <主题>`、`练习(Day N): <主题>`、`整理(Day N): <主题>`。

## 9. 确定性状态与工具

- 输入处理游标：`00-路线与状态/输入处理状态.json`
- 系统配置：`00-路线与状态/学习系统配置.json`
- 复习事实源：`06-复习区/复习索引.json`
- 调度工具：`scripts/learning_ops.py`

Agent负责语义判断，工具负责文件指纹、唯一 ID、日期计算、到期队列、复习历史、归档条件和状态校验。不得把今日复习 Markdown 队列当作排期事实源。

## 10. 停止条件

完成用户点名的动作后立即停止。若缺少实际记录、答案、运行结果或需要用户作出会改变路线的选择，应说明缺口并等待用户，不得凭空补齐。
